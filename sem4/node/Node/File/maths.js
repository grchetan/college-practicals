const add = (a, b) => a + b;
const sub = (a, b) => a - b;
const mul = (a, b) => a * b;
const div = (a, d) => a / b;
let name = "Chetan Prajapat";

let Student = {
  name: "Chetan",
  age: "20",
  subject: "BCA"
}
module.exports = {
  add,
  sub,
  mul,
  div,
  name,
  Student,
};
